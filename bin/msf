#!/bin/bash
set -euo pipefail

service postgresql start

msfdb reinit 2>/dev/null

if [ -n "${LIF:-}" ]; then
	LHOST="$(ifdata -pa "$LIF")"
	export LHOST
fi

exec /usr/bin/msfconsole -x "set -g CHOST 0.0.0.0" "$@"
